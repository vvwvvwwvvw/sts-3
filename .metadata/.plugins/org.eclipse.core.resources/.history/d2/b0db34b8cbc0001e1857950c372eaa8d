prefix + 파일명 + suffix

-> /WEB-INF/viwes/home.jsp

RequestMapping 과  GetMapping/ PostMapping 의차이
- RequestMapping 은 클래스와 메소드에서 모두 사용
- GetMapping/ PostMapping 은 메서드에서만 사용

분류된 카테고리 사이트의 경우 컨트롤러에 RequestMapping을
사용하여 기능을 분류하는 데 활용 가능

Static Resources(정적 자원 처리)
 image , css , js 저장위치
 	-webpp.resources 하위에 저장.